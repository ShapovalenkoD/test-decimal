import Decimal from "decimal.js";

export const leftMin = new Decimal(10000);
export const leftMax = new Decimal(70000000);
export const leftStep = new Decimal(100);

export const rightStep = new Decimal(0.01);
